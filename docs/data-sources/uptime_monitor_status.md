---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "fivenines_uptime_monitor_status Data Source - terraform-provider-fivenines"
subcategory: ""
description: |-
  Reads the current status of a single uptime monitor. This is the lightweight counterpart to the fivenines_uptime_monitor resource: it returns liveness fields only, which makes it cheap to refresh repeatedly.
---

# fivenines_uptime_monitor_status (Data Source)

Reads the current status of a single uptime monitor. This is the lightweight counterpart to the fivenines_uptime_monitor resource: it returns liveness fields only, which makes it cheap to refresh repeatedly.

## Example Usage

```terraform
resource "fivenines_uptime_monitor" "website" {
  name     = "Production Website"
  protocol = "https"
  url      = "https://example.com"
}

# Lightweight status read, cheap enough to refresh on every plan.
data "fivenines_uptime_monitor_status" "website" {
  id = fivenines_uptime_monitor.website.id
}

output "website_status" {
  # One of: unknown, up, down, paused, recovering.
  value = data.fivenines_uptime_monitor_status.website.status
}

output "website_certificate_expiry" {
  value = data.fivenines_uptime_monitor_status.website.ssl_expires_at
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) UUID of the uptime monitor to read.

### Read-Only

- `last_check_at` (String) Last check time.
- `last_error` (String) Last error message.
- `next_check_at` (String) Next scheduled check time.
- `paused` (Boolean) Whether the monitor is currently paused.
- `ssl_expires_at` (String) SSL certificate expiration date.
- `status` (String) Current status: "unknown", "up", "down", "paused" or "recovering".
